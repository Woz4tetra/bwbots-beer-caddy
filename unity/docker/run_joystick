#!/bin/bash
function cleanup {
    xhost -local:root
}
trap cleanup EXIT

HOST_MACHINE=$1
JOYSTICK_DEVICE=$2

xhost +local:root
docker run -it \
    --name workstation_joystick \
    --env="DISPLAY" \
    --env="QT_X11_NO_MITSHM=1" \
    --env="HOST_MACHINE=$HOST_MACHINE" \
    --restart="no" \
    --net="host" \
    --privileged \
    --stop-signal=SIGINT \
    -v=/home/${USER}/docker_ros_ws:/root/ros_ws:rw \
    -v=/home/${USER}/bwbots-beer-caddy/ros1/bwbots:/root/ros_ws/src/bwbots:rw \
    -v=/home/${USER}/bwbots-beer-caddy/unity/docker/resources/scripts:/root/scripts:rw \
    -v="/tmp/.X11-unix:/tmp/.X11-unix:rw" \
    --rm \
    bwbots_workstation:latest \
    /entrypoint.sh roslaunch bw_joystick bw_joystick.launch topic_name:=joy_remote device:=$JOYSTICK_DEVICE
